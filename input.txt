program demo;
const c := 2, limit := 10;
var n, result, temp;

procedure outer(x, z);
var i;
    procedure inner(y);
    begin
        if odd y then
            result := result + y
        else
            result := result - y
    end
begin
    i := 1;
    while i <= x do
    begin
        call inner(i);
        i := i + 1
    end
end;

procedure fact(k);
begin
    if k = 0 then
        result := 1
    else
    begin
        call fact(k - 1);
        result := result * k
    end
end

begin
    read(n, temp);
    result := 0;

    call outer(n + c, temp);
    write(result);

    call fact(temp);
    write(result)
end
